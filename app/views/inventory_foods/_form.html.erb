<!-- app/views/inventory_foods/_form.html.erb -->
<%= form_with(model: inventory_food, url: inventory_inventory_foods_path(@inventory)) do |form| %>

  <% if inventory_food.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(inventory_food.errors.count, "error") %> prohibited this inventory_food from being saved:</h2>
      <ul>
      <% inventory_food.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :food_id %>
    <%= form.collection_select :food_id, Food.all, :id, :name, prompt: true %>
  </div>

  <div class="field">
    <%= form.label :quantity %>
    <%= form.number_field :quantity %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
